
# script that pings 5 times an IP address passed as an argument and to display "Usage: 5-is_the_host_on_the_network {IP_ADDRESS}"
if [ "$#" -eq 0 ]; then
echo 'Usage: 5-is_the_host_on_the_network {IP_Address}'
else
ping -c 5 "$1"
fi
